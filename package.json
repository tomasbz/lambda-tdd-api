{
  "name": "lambda-tdd-api",
  "description": "Lambda tdd api example",
  "files": [
    "src",
    "*.js"
  ],
  "scripts": {
    "start": "claudia create --name lambda-tdd-api --region us-west-2 --api-module api --version dev",
    "deploy": "claudia update --version dev",
    "test": "mocha"
  },
  "dependencies": {
    "claudia-api-builder": "^1.0.2"
  }
}
